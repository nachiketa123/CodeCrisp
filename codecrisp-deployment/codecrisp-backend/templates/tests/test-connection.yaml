apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "codecrisp-backend.fullname" . }}-test-connection"
  labels:
    {{- include "codecrisp-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "codecrisp-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
