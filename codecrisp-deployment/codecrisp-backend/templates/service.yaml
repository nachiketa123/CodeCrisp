apiVersion: v1
kind: Service
metadata:
  name: {{ include "codecrisp-backend.fullname" . }}
  labels:
    {{- include "codecrisp-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "codecrisp-backend.selectorLabels" . | nindent 4 }}
